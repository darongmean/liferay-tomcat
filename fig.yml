web:
  build: .
#  links:
#    - "db:postgres"
  ports:
    - "8080:8080"
  volumes_from:
    - files
#db:
#  image: postgres:9.3.5
#  ports:
#    - "5432:5432"
#  volumes_from:
#    - files
files:
  image: busybox
  command: echo "files storage"
  volumes:
    - conf:/var/liferay
    - deploy:/opt/liferay-portal/deploy
    - /opt/liferay-portal/data    
    - /var/lib/postgresql/data
